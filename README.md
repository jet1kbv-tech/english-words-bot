# English Words Bot

MVP Telegram-бота для совместного изучения английских слов двумя пользователями: Вовой (`@wp_bvv`) и Сашей (`@privetnormalno`). Бот работает как отдельный Python-проект и может быть запущен локально или как отдельный `systemd`-сервис на сервере.

## Возможности

- Доступ только для разрешённых Telegram username при первом входе.
- После регистрации пользователь хранится и определяется по `telegram_id`, поэтому смена username не ломает доступ.
- Добавление слов и фраз с переводом, опциональной темой и примером.
- Массовое добавление слов списком.
- Удаление слов из личного словаря.
- Личный словарь пользователя с пагинацией по 10 слов в одном сообщении.
- Режим `🔄 Обмен словами`: пользователь тренирует слова партнёра, а незнакомые слова добавляются в его личный словарь.
- Тренировка по своим словам отдельным режимом `🎯 Мои карточки`: после выбора `✅ Помню` / `❌ Не помню` бот автоматически показывает правильный ответ; после положительного ответа можно нажать `😬 Ошибся`, чтобы исправить результат на `❌ Не помню`.
- Короткая игровая сессия `🎮 Игра на 10 слов`: 10 карточек из личного словаря с миксом новых, слабых и небольшого числа сильных слов; каждая карточка просит ввести ответ в чат. Направление перевода выбирается случайно, бот проверяет ответ через AI при наличии настройки, иначе использует fallback-проверку, и предлагает `😬 Я был прав`, если правильный вариант был засчитан как ошибка.
- Smart review для `🎯 Мои карточки` и `🔄 Обмен словами`: новые слова появляются чаще, забытые слова появляются чаще, а хорошо знакомые слова появляются реже.
- Отдельный прогресс для каждой пары `user_id` + `word_id`.
- SQLite-база создаётся автоматически при первом запуске.
- Учёт `study_sessions`, `daily_activity`, streak и уровня дня.
- Ежедневное напоминание в 14:00 Europe/Moscow через JobQueue.

## Структура проекта

```text
.
├── main.py
├── requirements.txt
├── .env.example
├── app
│   ├── config.py
│   ├── database.py
│   ├── keyboards.py
│   └── handlers
│       ├── start.py
│       ├── menu.py
│       ├── words.py
│       └── training.py
└── README.md
```

## Локальный запуск

Требуется Python 3.11+.

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
```

Заполните `.env` реальным токеном Telegram-бота:

```dotenv
BOT_TOKEN=123456:replace-with-real-token
DATABASE_PATH=english_words_bot.sqlite3
LOG_LEVEL=INFO
```

Запуск:

```bash
python main.py
```

## Переменные окружения

| Переменная | Описание | Значение по умолчанию |
| --- | --- | --- |
| `BOT_TOKEN` | Telegram Bot API token. Обязательная переменная. | — |
| `DATABASE_PATH` | Путь до SQLite-файла. | `english_words_bot.sqlite3` |
| `LOG_LEVEL` | Уровень логирования Python. | `INFO` |
| `OPENAI_API_KEY` | Зарезервировано для будущих AI-функций. Сейчас не используется. | — |


## Seed words

Одноразовый импорт списка слов в личный словарь Саши (`@privetnormalno`) из раздела CHICAGA «Слова на английском языке для расширения словарного запаса» и следующих таблиц. Перед запуском Саша должен открыть `/start` в боте, чтобы пользователь появился в базе. Скрипт читает `DATABASE_PATH` из `.env` и пропускает уже добавленные слова.

```bash
python scripts/seed_sasha_chicaga_words.py
```

Старый импорт для Вовы (`@wp_bvv`) также доступен:

```bash
python scripts/seed_vova_words.py
```

## Запуск на сервере через systemd

Пример размещения проекта: `/opt/english-words-bot`.

```bash
cd /opt/english-words-bot
python3.11 -m venv .venv
. .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
nano .env
```

Пример unit-файла `/etc/systemd/system/english-words-bot.service`:

```ini
[Unit]
Description=English Words Telegram Bot
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
WorkingDirectory=/opt/english-words-bot
EnvironmentFile=/opt/english-words-bot/.env
ExecStart=/opt/english-words-bot/.venv/bin/python main.py
Restart=always
RestartSec=5
User=englishbot
Group=englishbot

[Install]
WantedBy=multi-user.target
```

Команды управления сервисом:

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now english-words-bot
sudo systemctl status english-words-bot
journalctl -u english-words-bot -f
```

## Схема SQLite

Бот создаёт таблицы:

- `users`: `id`, `telegram_id`, `username`, `display_name`, `created_at`, `updated_at`.
- `words`: `id`, `owner_user_id`, `english`, `translation`, `topic`, `example`, `created_at`, `updated_at`.
- `word_progress`: `id`, `user_id`, `word_id`, `score`, `times_seen`, `times_remembered`, `times_forgotten`, `last_reviewed_at`, `created_at`, `updated_at`, уникальная пара `(user_id, word_id)`.
- `study_sessions`: игровые сессии пользователя, количество карточек, известные/неизвестные/пропущенные карточки, время старта и завершения.
- `daily_activity`: дневная активность пользователя, количество повторённых карточек, streak и уровень дня.

## Команды и меню

- `/start` — регистрация/обновление пользователя и показ главного меню.
- `➕ Добавить слово` — пошаговое добавление слова.
- `📥 Добавить список слов` — массовое добавление слов из нескольких строк.
- `📚 Мой словарь` — только слова текущего пользователя, постранично по 10 слов; кнопки `⬅️ Назад` и `➡️ Далее` редактируют текущее сообщение.
- `🗑 Удалить` в личном словаре — показывает слова текущей страницы кнопками `🗑 1`, `🗑 2`, ... и удаляет только слова текущего пользователя после подтверждения.
- `🎯 Мои карточки` — тренировка по своим словам.
- `🎮 Игра на 10 слов` — короткая сессия по личному словарю: новые и слабые слова имеют приоритет, часть сильных слов добавляется для закрепления; каждая карточка показывает слово или перевод и просит написать ответ в чат. Направление случайное: English → Russian или Russian → English; бот проверяет ответ через AI при наличии настройки, иначе fallback-проверка учитывает регистр, лишние пробелы, `ё/е` и варианты перевода через `/`, `,`, `;`. После 10 карточек бот показывает итог, streak и уровень дня.
- `🔄 Обмен словами` — тренировка по словам партнёра: Вова видит слова Саши, Саша видит слова Вовы. Если нажать `❌ Не знаю`, бот сначала показывает правильный ответ, а затем копирует слово в личный словарь текущего пользователя без дублей по `owner_user_id + lower(english)` и сообщает статус добавления.
- `📊 Прогресс` — статистика текущего пользователя.

Карточки выбираются через weighted random с учётом прогресса текущего пользователя: слова без прогресса получают самый высокий приоритет, слова со сниженным score показываются чаще, а слова со score 3 и выше — реже. В режиме обмена бот не показывает слова партнёра, которые уже есть в личном словаре текущего пользователя.

Карточки работают в двух направлениях и для каждой карточки направление выбирается случайно: English → Russian и Russian → English. После выбора `✅ Помню` / `❌ Не помню` или `✅ Знаю` / `❌ Не знаю` бот автоматически показывает правильный ответ; после положительного ответа дополнительно предлагает `😬 Ошибся` для исправления статистики, а также `➡️ Следующая карточка` или `🛑 Закончить`; кнопка `👀 Показать ответ` остаётся доступной для ручной проверки до выбора.

В игровом режиме каждая карточка работает как ввод ответа: бот просит `✍️ Напиши ответ в чат`, затем показывает `✅ Верно` или `❌ Не совсем`, правильный ответ, пример при наличии и кнопки `➡️ Следующая карточка` / `🛑 Закончить`; после неверного автозачёта доступна кнопка `😬 Я был прав`, которая переносит карточку из «не знаю» в «знаю» без повторного увеличения `times_seen`.

Во время личной тренировки доступны кнопки: `👀 Показать ответ`, `✅ Помню`, `❌ Не помню`, `⏭ Пропустить`, `🛑 Закончить`. В режиме обмена доступны кнопки: `👀 Показать ответ`, `✅ Знаю`, `❌ Не знаю`, `⏭ Пропустить`, `🛑 Закончить`; если после `✅ Знаю` нажать `😬 Ошибся`, слово добавится в личный словарь так же, как при `❌ Не знаю`, без дублей.


## Future AI features

Планируемые возможности без текущего подключения OpenAI SDK и без реальных API-вызовов:

- автоперевод слова;
- генерация примеров;
- массовый импорт списка английских слов без переводов;
- автоматическое определение темы;
- мини-тесты и упражнения.
